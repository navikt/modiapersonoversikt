import styled from 'styled-components';

const FillAndCenterDiv = styled.div`
    flex-grow: 1;
    display: flex;
    justify-content: center;
    align-items: center;
`;

export default FillAndCenterDiv;
